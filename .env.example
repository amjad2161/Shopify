# Copy to .env for local development. Never commit .env.
SESSION_SECRET=replace-with-random-string

# After linking a real store (shopify auth login && npx shopify hydrogen link):
# PUBLIC_STORE_DOMAIN=your-store.myshopify.com
# PUBLIC_STOREFRONT_API_TOKEN=your-storefront-token
# PRIVATE_STOREFRONT_API_TOKEN=optional
# PUBLIC_STOREFRONT_ID=optional
# PUBLIC_CHECKOUT_DOMAIN=optional
